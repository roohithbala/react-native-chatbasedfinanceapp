import React from 'react';
import BudgetHeader from '@/app/components/BudgetHeader';

type Props = any;

export default function BudgetHeaderWrapper(props: Props) {
  return <BudgetHeader {...props} />;
}
